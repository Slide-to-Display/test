# Because Display.py contains only one function Display(data):
# This function is essentially a function that calls functions from classes that
# were previously written
# Therefore, the unit testings were done before Display was written
